//내가 구현하는 게 아니라 C++ 내부에서 구현되는거지 ?
//ChatGPT
//네, 맞습니다.가상 함수 표(vtable)와 vptr(가상 함수 테이블 포인터)는 C++ 컴파일러가 다형성을 지원하기 위해 내부적으로 생성하고 관리하는 구조입니다.개발자는 이 구조를 직접 구현하지 않고, 단지 가상 함수(virtual 키워드)를 사용하여 다형성을 구현할 수 있습니다.
//
//요약
//컴파일러의 역할 : C++ 컴파일러는 가상 함수를 포함하는 클래스에 대해 vtable과 vptr을 자동으로 생성하고 관리합니다.
//개발자의 역할 : 개발자는 virtual 키워드를 사용하여 가상 함수를 선언하고, 자식 클래스에서 이를 재정의하여 다형성을 구현합니다.
//내부 동작 : 컴파일러는 클래스 정의 시 vtable을 생성하고, 객체 생성 시 vptr을 설정하여 올바른 vtable을 가리키게 합니다.가상 함수 호출 시 vptr을 통해 vtable에 접근하여 적절한 함수가 호출됩니다.